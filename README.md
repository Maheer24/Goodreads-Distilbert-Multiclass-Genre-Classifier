# Goodreads-Distilbert-Multiclass-Genre-Classifier
A fine-tuned DistilBERT model for multi-class genre classification using book summaries scraped from Goodreads. The project includes data preprocessing, model training, evaluation, and deployment as a Streamlit app.
